# Docker Runtime Knowledge

- Compose is the developer interface.
- Named volumes hold runtime state.
- The host should not need language toolchains.
- Commands in docs must run through services.
- Logs and health checks are proof sources only after they run.
- Secrets must stay out of source control.
